# common gitignore for all projects
#
# This file is used by git to ignore files that are generated by
# the build system.  It is not used by the build system itself.
# It is used by git to ignore files that are generated by the
# build system.  It is not used by the build system itself.

# Ignore all files in the build directory
build/

# Ignore all files in the dist directory
dist/
node_modules
